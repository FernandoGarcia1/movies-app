import { ImgOpacityDirective } from './img-opacity.directive';

describe('ImgOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
